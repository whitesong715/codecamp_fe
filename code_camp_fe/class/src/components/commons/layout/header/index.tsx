import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 30px;
  background-color: yellow;
`;

export default function LayoutHeader(): JSX.Element {
  return <Wrapper>여기는 헤더</Wrapper>;
}

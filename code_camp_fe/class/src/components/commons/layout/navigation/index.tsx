import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 100px;
  background-color: #4f9dca;
`;

export default function LayoutNav(): JSX.Element {
  return <Wrapper>여기는 내비게이션</Wrapper>;
}
